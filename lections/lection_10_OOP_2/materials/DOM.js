// console.dir(document);

// --------------------------


window.onload = function(){
    var block = document.querySelector('div.block');

    setTimeout(function() {

        block.classList.add("hello");
        block.classList.remove("kjdf");
sole.log(block.classList.contains("hello")
        con)
    }, 1500);

    // block.className += " hello";
    

    console.log(block.className)
    // console.dir(document.querySelector('.elem'));
    console.dir(block);
}